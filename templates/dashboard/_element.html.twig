<a href="{{ c_link }}"
   class="block p-1 hover:bg-gray-100 dark:hover:bg-zinc-500 rounded-md">
    <div class="flex items-center justify-between">
        <div class="flex items-center">
            <img src="{{ asset(c_image_path) }}" alt="{{ c_alt }}" class="w-4 h-4">
            <span class="ml-2">{{ c_name }}</span>
        </div>
        <span class="px-2 bg-zinc-100 dark:bg-zinc-500 text-zinc-400 dark:text-zinc-300 rounded-md">{{ c_count }}</span>
    </div>
</a>